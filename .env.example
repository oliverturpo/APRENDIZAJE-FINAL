# =============================================================================
# PLANTILLA DE CONFIGURACIÓN - Copiar a .env y completar
# =============================================================================

# ----- BASE DE DATOS (PostgreSQL) -----
DB_HOST=localhost
DB_PORT=5432
DB_NAME=neoauto
DB_USER=postgres
DB_PWD=TU_PASSWORD_AQUI

# ----- DJANGO SETTINGS -----
# Generar con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=GENERA_TU_SECRET_KEY_AQUI

# Modo debug (False en producción)
DEBUG=True

# Hosts permitidos (separados por coma, sin espacios)
# Desarrollo: localhost,127.0.0.1
# Producción: tu-dominio.com,www.tu-dominio.com,167.99.172.218
ALLOWED_HOSTS=localhost,127.0.0.1

# ----- CORS (Para que React pueda conectarse) -----
# Desarrollo: http://localhost:5173,http://localhost:3000
# Producción: http://TU_IP:5173,http://TU_IP,https://tu-dominio.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
